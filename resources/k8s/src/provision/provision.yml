---
- name: Wait for k8s-main VM to boot and become reachable
  hosts: all
  gather_facts: false
  connection: local

  tasks:
    - name: Wait for SSH to come up on the new VM
      wait_for:
        host: "{{ inventory_hostname }}"
        port: 22
        delay: 10
        timeout: 300
