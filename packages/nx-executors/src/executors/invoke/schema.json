{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "additionalProperties": true,
  "required": ["module"],
  "properties": {
    "module": {
      "type": "string",
      "description": "Module to require. Will invoke default export with configuration API."
    },
    "cwd": {
      "type": "string",
      "description": "Current working directory in which search for module."
    },
    "env": {
      "type": "string",
      "description": "Cluster environment to use. Will be passed to the module."
    }
  }
}
