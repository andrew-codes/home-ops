---
apiVersion: v1
kind: Service
metadata:
  name: voice-agent-game-room
  namespace: default
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
  selector:
    app: voice-agent-game-room
  type: ClusterIP
